# ===========================================
# Database (Supabase)
# ===========================================
# Local dev (Supabase CLI):
# DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
#
# Production (Supabase pooler, session mode):
DATABASE_URL=

# ===========================================
# BetterAuth
# ===========================================
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# App URL (must match trustedOrigins in lib/auth.ts)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# ===========================================
# BunnyCDN (only if features.fileUploads = true)
# ===========================================
BUNNY_STORAGE_ZONE=
BUNNY_STORAGE_API_KEY=
# LA region: https://la.storage.bunnycdn.com
BUNNY_STORAGE_ENDPOINT=https://storage.bunnycdn.com
NEXT_PUBLIC_BUNNY_CDN_URL=

# ===========================================
# OpenRouter (only if features.aiImages = true)
# ===========================================
OPENROUTER_API_KEY=
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_APP_NAME=MyProject

# ===========================================
# Production SSL
# ===========================================
# Generate with: base64 -i supabase-ca.crt | tr -d '\\n'
# SUPABASE_CA_CERT=
